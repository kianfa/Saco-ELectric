import {
  manualCheckoutConfig,
  manualCheckoutExplanation,
  manualCheckoutHelperText,
} from "@/lib/store-contact-config"

export { manualCheckoutConfig, manualCheckoutExplanation, manualCheckoutHelperText }
export type ManualCheckoutConfig = typeof manualCheckoutConfig
